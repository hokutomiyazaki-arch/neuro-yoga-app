# WORKLOG — neuro-yoga-app

## §0 いまここ

**ニューロサイエンスヨガの音声ガイドPWA。** 23ポーズ・音声69本。
無料は3ポーズ（No.1 / No.6 / No.24）、残りは解除コードで開く。

- 正本は `~/dev/neuro-yoga`。ここは **`build_app.py` が作る生成物**
- **まだ公開していない**（GitHub Pages は未設定。宮崎さんのGO待ち）
- 解除コードは **仮の `NEUROYOGA23`**。宮崎さんが決めた語に差し替える

### 次にやること

1. 解除コードを決める → `build_app.py` の `CODE` を直して `--force` で作り直す
2. GitHubへ publish（**public**。Pagesで配信するため）→ Pages を有効化
3. 実機（iPhone / iPad）で通しテスト。とくに **①→②→③ の連続再生が2本目以降も鳴るか**
4. LINEの自動応答からアプリのURLを配る導線をつなぐ

## 決めたこと

- **技術・骨格は Notion「🧠 FNT PWA開発ナレッジ」に従う**（3ファイル構成・Network First・
  min-height・touch-action・Audio要素の使い回し・アプリ固有アイコン）
- **ブランドはニューロサイエンスヨガ**（共同事業なので FNT ではない）。
  色は LP と同じ濃紺 `#0a0f28` ＋ 金 `#d8bd83`。アイコンは LINE と同じロゴ。
  著作権は `© 2026 Le grand chariot`、その下に小さく `Functional Neuro Training`
- **鍵は「画面で隠す」ではなく「ファイル名を隠す＋中身を封じる」**。
  公開リポジトリなので、UIで隠してもURLを叩けば聞けてしまう。
  → 鍵つきのファイル名と台本は暗号化して `data.js` に入れた（サーバー不要）
- **音声は 192kbps → 64kbps モノラル、絵は PNG → WebP(横900px)**。
  84MB → 17MB。PWAで配るので容量がそのまま初回の重さになる
- **プリキャッシュは無料3ポーズ（約2MB）まで。** 残りは再生したときにキャッシュへ落とす。
  17MB を install で取ると、失敗したとき SW ごと入らない

## やったこと

- 2026-09-22 アプリを新規作成。`~/dev/neuro-yoga/build_app.py` で素材とデータを生成。
  ヘッドレスChromeで描画を確認（横はみ出しなし）。解除の暗号は Node で往復を検算
